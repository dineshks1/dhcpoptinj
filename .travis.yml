addons:
   apt:
      update: true
      packages:
         - libnetfilter-queue-dev
dist: xenial
arch:
 - amd64
 - ppc64le
language: c
compiler:
   - clang
   - gcc
before_script:
   - mkdir build
script:
   - cd build
   - cmake ..
   - make -j2
